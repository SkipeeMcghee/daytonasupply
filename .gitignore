# Local secrets & configuration.  Prevent committing environment-specific
# overrides such as SMTP credentials.
includes/config.local.php

# Logs & temporary files
logs/
tmp/
cache/
assets/cache/

# Development email output (written by sendEmail dev fallback)
data/emails/

# User uploads (if added in the future)
uploads/

# Operating system and IDE generated files
.DS_Store
Thumbs.db
.idea/
.vscode/