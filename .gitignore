# ── Local secrets & config ────────────────────────────────────────────────────
includes/config.local.php
includes/PHPMailer/        # only if you manually dropped PHPMailer files

# ── SQLite database & journals (keep data out of Git) ────────────────────────
data/*.sqlite
data/*.db
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.sqlite-journal

# ── Composer artifacts ───────────────────────────────────────────────────────
/vendor/
/composer.phar
# (Keep composer.lock tracked for reproducible installs)

# ── Logs & temp ──────────────────────────────────────────────────────────────
logs/
*.log
tmp/
cache/
assets/cache/

# ── Uploads (if you add user uploads later) ─────────────────────────────────
uploads/

# ── OS/editor junk ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
